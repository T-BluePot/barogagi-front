/**
 * 세션 종료 안내 문구.
 *
 * 카피 규칙: 존댓말 요-체(`~어요 / ~세요`). 단정형 `~다` 금지 (`.claude/design/DESIGN.md` §10)
 */
export const SESSION_TEXT = {
  /**
   * 사용자가 직접 로그아웃한 게 아닌데 로그인 화면으로 돌아왔을 때.
   *
   * 사유를 특정하지 않는다 — 리프레시 토큰 만료가 가장 흔하지만, 다른 기기에서 탈퇴해
   * 세션이 끊긴 경우도 같은 경로로 들어온다. 어느 쪽이든 사용자가 할 일은 재로그인이라
   * 원인을 나열하기보다 다음 행동만 알린다.
   */
  FORCED_LOGOUT: "로그인이 만료되었어요. 다시 로그인해 주세요.",
} as const;
